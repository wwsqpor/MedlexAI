export const selectCases = (state) => state.cases.cases;

export const selectCategories = (state) => state.cases.categories;

export const selectCasesStatus = (state) => state.cases.casesStatus;
export const selectCategoriesStatus = (state) => state.cases.categoriesStatus;
export const selectCaseDetailsStatus = (state) => state.cases.caseDetailsStatus;

export const selectError = (state) => state.cases.error;

export const selectCaseDetails = (state) => state.cases.caseDetails;